import logo from '../assets/logo.svg';

const Logo = () => {
  return (
    <img
      src={logo}
      alt="Logo CMS-Ticket"
      className="w-[133px] h-[60px] object-contain mb-10"
    />
  );
};

export default Logo;
